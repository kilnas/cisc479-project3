@import 'styles/load';

// #zen-intro
//   @extends $l-col-1-3
//   header
//     @extends $l-col-1-3
//   .summary
//     @extends $l-col-2-3
//   .preamble
//     @extends $l-col-3-3

// #zen-supporting
//   @extends $l-col-2-3

// aside.sidebar
//   @extends $l-col-3-3

body
  @extends $teeny;
  //squarify(20)
  line-height: .7em;
  //@extends $right;
  @extends $text-left;
  @extends $left;
  height: 800px;
  div
    h-train-car-inline(30, 0,0)
  h1
    @extends $blind
    @extends $left;
    @extends $profile-pic